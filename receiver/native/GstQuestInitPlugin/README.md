# GstQuestInitPlugin

This folder contains the Android ARM64 native plugin source used by the Quest Unity receiver.

Build locally with Android NDK:

```bat
ndk-build
```

Generated binaries should not be committed to the source repository unless intentionally managed through Git LFS or a release package.
