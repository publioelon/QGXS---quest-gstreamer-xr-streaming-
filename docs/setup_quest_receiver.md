# Quest receiver setup

Open `receiver/unity/QuestGStreamerReceiver/` in Unity 2022.3.x.

Build the Android native plugin separately and copy the generated ARM64 shared libraries into the Unity Android plugin folder for local builds.
