# Launcher usage

Run the launcher from the `launcher/` directory.

```bat
python launcher_gui.py
```

Create a local `config.ini` from `config.example.ini` if needed. Do not commit local configuration files.
